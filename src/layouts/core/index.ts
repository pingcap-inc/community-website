// CoreLayout was the legacy header footer layout component, which was replaced by new SiteLayout
// TODO: Old red dots
//   const { data: redDotsResp } = useSWR(isLoggedIn && 'operation.fetchRedDots');
//   const redDots = redDotsUtils.transformRespToMap(redDotsResp);
export { default } from '../site';
