const MESSAGES = {
  SUCCESS: 'success',
  INVALID_PARAMS: 'params wrong',
  TOO_MANY_REQUESTS: 'rate limit',
};

const ROLES = {
  ADMIN: 'admin',
  MEMBER: 'member',
};

module.exports = {
  MESSAGES,
  ROLES,
};
