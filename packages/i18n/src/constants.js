const locales = ['zh', 'en'];

const allHeaders = ['Lang Key', ...locales];

const colors = {
  black: 'ff000000',
  blue: 'ff3d3fea',
  yellow: 'ffffff00',
};

module.exports = {
  locales,
  allHeaders,
  colors,
};
